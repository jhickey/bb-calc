# Publish auth is injected by CI into ~/.npmrc (see .github/workflows/CI.yml).
# Do not commit registry credentials here — pnpm ignores expandable secrets
# in a project-level .npmrc to avoid leaking them to untrusted registries.
