# Supabase client config. Both are public (safe to ship — RLS protects data).
# Get them from `supabase projects api-keys` or the dashboard after the
# Terraform project is applied (see infra/terraform/README.md).
VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxx
