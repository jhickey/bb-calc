
[package]
authors = ["Jimmy Hickey"]
edition = "2021"
name    = "bb_calc_js"
version = "0.2.0"

[lib]
crate-type = ["cdylib"]

[dependencies]
bb-calc     = { path = "../bb-calc" }
napi        = "3.0.0"
napi-derive = "3.0.0"

[build-dependencies]
napi-build = "2"

# Release profile lives in the workspace root Cargo.toml; Cargo ignores
# profiles declared in non-root workspace members.
